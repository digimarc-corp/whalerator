# Content queue worker, downloads and indexes layers to retrieve content
cache:
  redis: redis
  volatileTtl: 3600 # 1 hour
contentScanner:
  layercache: /var/data
security:
  privateKey: key.pem
logging:
  logLevel:
    default: "Information"
